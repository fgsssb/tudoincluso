const form=document.getElementById('loginForm');const msg=document.getElementById('loginMsg');function toast(t){const el=document.getElementById('toast');el.textContent=t;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2600)}form.addEventListener('submit',async e=>{e.preventDefault();msg.textContent='';const login=document.getElementById('login').value.trim();const senha=document.getElementById('senha').value;try{const r=await fetch('/api/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({login,senha})});const data=await r.json();if(!r.ok)throw new Error(data.error||'Erro ao entrar');localStorage.setItem('ti_user',JSON.stringify(data.user));location.href='/app.html'}catch(err){msg.textContent=err.message;toast('Não foi possível entrar')}});
